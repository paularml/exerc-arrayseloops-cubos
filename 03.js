// Encontre o 10
// Crie um array de números inteiros.
// Faça um programa que verifica se existe o número 10 nesse array. Caso exista, 
// informa a posição (índice) em que o número 10 se encontra. Caso não exista, 
// deverá ser impresso -1.

const numeros = [1, 2, 3, 10, 4, 10, 5]

for (let contador of numeros) {
    if (contador === 10) {
        numeros.length[10]
    }
}